services:
  postgres:
    image: postgres:16
    container_name: golf-tracker-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    env_file:
      - .env.db
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
